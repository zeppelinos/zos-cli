"use strict";

const App = artifacts.require('App');

import Deployer from '../model/Deployer';

contract('Deploy', (accounts) => {
  describe.only('AppManager', function() {

    it('can create a new AppManager for a project', async function() {

    });
  });
 });
